``` lua
function main_menu_items(items,last_id)
    --body
    return last_id + n
end
```
```n``` - number of **id** used

# Argumets
- *item* - ```table``` with menu items
- *last_id* - number id occupied, use ```last_id + n``` for elements.
# Returns
- *number* last_id - number id occupied
